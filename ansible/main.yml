---
- name: '-'
  hosts: all
  gather_facts: false
  become_method: sudo

  vars:
    <VARS>

  pre_tasks:
    - name: 'Connect to host & retrieve facts'
      ansible.builtin.setup:
        gather_subset:
          - '!all'
          - 'network'

  roles:
    - role: GITHUB.UPDATE.ROLE

  tasks:
    - import_tasks: tasks/<TASKS>
